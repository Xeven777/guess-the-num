
import React from "react";

type Props = {
  params: { id: string };
};

const Page = ({ params: { id } }: Props) => {


  return (
    <div>
      <p className="text-white">  </p>
    </div>
  );
};

export default Page;
